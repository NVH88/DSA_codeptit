#include<bits/stdc++.h>
using namespace std;

int n, k, a[20], ok;

void Try(int i, int sum, string s) {
	for(int j = i; j < n; ++j) {
		if(sum + a[j] <= k) {
			if(sum + a[j] == k) {
				ok = 1;
				cout << s << a[j] << "] ";
			}
			else Try(j + 1, sum + a[j], s + to_string(a[j]) + " "); 
		}
		else return;
	}
}

int main() {
	int t; cin >> t;
	while(t--) {
		ok = 0;
		cin >> n >> k;
		for(int i = 0; i < n; i++) cin >> a[i];
		sort(a, a + n);
		Try(0, 0, "[");
		if(!ok) cout << -1;
		cout << endl;
	}
}
