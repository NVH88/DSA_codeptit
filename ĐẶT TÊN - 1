#include<bits/stdc++.h>
using namespace std;

int n, k, a[100];
vector<string> v;

void Try(int i) {
	for(int j = a[i - 1] + 1; j <= n - k + i; ++j) {
		a[i] = j;
		if(i == k) {
			for(int p = 1; p <= i; ++p) {
				cout << v[a[p] - 1] << " ";
			}
			cout << endl;
		}
		else Try(i + 1);
	}
}

int main() {
	cin >> n >> k;
	set<string> se;
	for(int i = 0; i < n; ++i) {
		string s; cin >> s;
		se.insert(s);
	}
	for(string j : se) {
		v.push_back(j);
	}
	n = v.size();
	Try(1);
}
