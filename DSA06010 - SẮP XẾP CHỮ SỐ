#include<bits/stdc++.h>
using namespace std;

int main() {
	int t;
	cin >> t;
	while(t--) {
		int n;
		cin >> n;
		set<int> se;
		for(int i = 0; i < n; i++) {
			long long a;
			cin >> a;
			while(a) {
				se.insert(a % 10);
				a /= 10;
			}
		}
		for(int i : se) cout << i << " ";
		cout << endl;
	}
}
