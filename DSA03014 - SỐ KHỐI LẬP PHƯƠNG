#include<bits/stdc++.h>
using namespace std;
#define ll long long

vector<ll> v;

string calc(string s) {
	for(int i = 0; i < 100; i++) {
		string tmp = to_string(v[i]);
		int cnt = 0, idx = 0;
		for(int j = 0; j < s.length(); j++) {
			if(idx < tmp.length() && s[j] == tmp[idx]) {
				++cnt;
				++idx;
			}
		}
		if(cnt == tmp.length()) return tmp;
	}
	return to_string(-1);
}

int main() {
	for(int i = 100; i >= 1; i--) v.push_back(i * i * i);
	int t; cin >> t;
	while(t--) {
		string s; cin >> s;
		cout << calc(s) << endl;
	}
}
