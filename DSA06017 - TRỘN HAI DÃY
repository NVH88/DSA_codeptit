#include<bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
	int t;
	cin >> t;
	while(t--) {
		int n, m;
		cin >> n >> m;
		multiset<ll> ms;
		for(int i = 0; i < m + n; i++) {
			ll a; cin >> a;
			ms.insert(a);
		}
		for(ll i : ms) cout << i << " ";
		cout << endl;
	}
}
