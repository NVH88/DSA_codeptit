#include<bits/stdc++.h>
using namespace std;

int main() {
	int t; cin >> t;
	while(t--) {
		int n; cin >> n;
		set<int> se;
		for(int i = 0; i < n; ++i) {
			int a, b; char c; cin >> a >> b >> c;
			se.insert(a); se.insert(b);
		}
		for(int i : se) cout << i << " ";
		cout << endl;
	}
}
