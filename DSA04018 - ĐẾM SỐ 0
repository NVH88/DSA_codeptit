#include <bits/stdc++.h>
using namespace std;
#define ll long long
const int mod = 1e9 + 7;

int main() {
	int t; cin >> t;
	while(t--) {
	    int n;
	    cin >> n;
	    int a[n];
	    for(int &i : a) cin >> i;
	    auto it = lower_bound(a, a + n, 1);
	    cout << it - a << endl;
	}
}
