#include<bits/stdc++.h>
using namespace std;

int main() {
	int t; cin >> t;
	while(t--) {
		int n; cin >> n;
		int a[n], b[n]; map<int, int> mp;
		for(int i = 0; i < n; ++i) {
			cin >> a[i]; ++mp[a[i]];
		}
		stack<int> st;
		for(int i = n - 1; i >= 0; --i) {
			while(st.size() && mp[a[i]] >= mp[a[st.top()]]) st.pop();
			if(!st.size()) b[i] = -1;
			else b[i] = st.top();
			st.push(i);
		}
		for(int i = 0; i < n; ++i) {
			if(b[i] != -1) cout << a[b[i]] << " ";
			else cout << -1 << " ";
		}
		cout << endl;
	}
}
