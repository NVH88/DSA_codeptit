#include<bits/stdc++.h>
using namespace std;

int main() {
	int t; cin >> t;
	while(t--) {
		int n; cin >> n;
		int ans = 0, bac[n + 1] = {0};
		for(int i = 0; i < n - 1; ++i) {
			int a, b; cin >> a >> b;
			bac[b] = bac[a] + 1;
			ans = max(ans, bac[b]);
		}
		cout << ans << endl;
	}
}

