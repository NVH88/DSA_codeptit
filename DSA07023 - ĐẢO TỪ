#include<bits/stdc++.h>
using namespace std;

int main() {
	int t; cin >> t;
	cin.ignore();
	while(t--) {
		string s, tmp; getline(cin, s);
		stringstream ss(s);
		stack<string> st;
		while(ss >> tmp) {
			st.push(tmp);
		}
		while(st.size()) {
			cout << st.top() << " ";
			st.pop();
		}
		cout << endl;
	}
}
