#include<bits/stdc++.h>
using namespace std;

int main() {
	int t; cin >> t;
	while(t--) {
		int n;
		cin >> n;
		int a[n + 1];
		a[0] = 0;
		bool ok = true;
		for(int i = 1; i <= n; ++i) {
			cin >> a[i];
			if(a[i] <= a[i - 1]) ok = false;
		}
		cout << ok << endl;
	}
}
