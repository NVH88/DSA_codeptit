#include<bits/stdc++.h>
using namespace std;

int main() {
	stack<int> st;
	string s;
	while(cin >> s) {
		if(s == "show") {
			if(st.empty()) {
				cout << "empty" << endl;
			}
			else {
				vector<int> v;
				while(!st.empty()) {
					v.push_back(st.top()); st.pop();
				}
				reverse(v.begin(), v.end());
				for(int i : v) {
					cout << i << " ";
					st.push(i);
				}
				cout << endl;
			}
		}
		else {
			if(s == "pop") {
				if(st.empty()) continue;
				else st.pop();
			}
			else {
				int x;
				cin >> x;
				st.push(x);
			}
		}
	}
}
